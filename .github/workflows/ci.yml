name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Print env versions (debug)
        run: |
          python --version
          python -c "import sys; print('platform:', sys.platform)"
          python -c "import typing_extensions; print('typing_extensions:', typing_extensions.__version__)"
          python -c "import pydantic; print('pydantic:', pydantic.__version__)"
          python -c "import pydantic_core; print('pydantic_core:', pydantic_core.__version__)"

      - name: Lint
        run: ruff check fatture_cli/

      - name: Type check
        run: mypy fatture_cli/
        continue-on-error: true

      - name: Test
        run: pytest tests/ --cov=fatture_cli --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.python == '3.12'
        uses: codecov/codecov-action@v4
        with:
          files: coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
